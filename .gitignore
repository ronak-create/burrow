# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch notes — personal working files, not project docs
*.local.md

# Local speech tooling — whisper.cpp binaries, a ~140MB model, and rendered test
# fixtures. Downloaded on demand, never part of the repo: the whole point of the
# local STT design is that no binary or model ships with the app.
/tools/
